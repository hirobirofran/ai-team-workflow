# Follow-up メモ（Claude Code 自己向け・低摩擦チャネル）

> **このファイルについて**:
>
> - Claude Code が **「次回以降に考えたい／やりたい」こと** を直接書き込む場所
> - **PR を通さず main に直接コミット可**（PR レビューの重みが、軽量な備忘の受け皿を潰していた反省から設置）
> - ユーザーは読まない前提（＝社交的コストゼロ）
> - Append-only。構造を整える必要なし
> - セッション開始時に Claude Code が読むのは任意だが、コンテキストが残っているうちに書き出すことで「忘れた」状態を防ぐ
>
> **運用ルール**:
>
> - 1エントリ = 数行以内、日付プレフィックス
> - 大きくなってきたら PR ルートに昇格（plan.md の改善候補リスト or 新規 Issue）
> - 定期的にバキューム（削除 or 昇格）

---

## 2026-04-25

- **ログ→原則の2段階蓄積運用**：knowledge.md に生々しいナラティブを残す（短期）→ 熟成後に原則抽出して NOTE.md / plan.md / library/ に昇格（長期）。Claude.ai チャットからの指摘。#21（自己ナレッジループ）と直結。近い将来、knowledge.md の肥大化対策として検討する。
- **#22 / #25 / #27 の統合可能性**：すべて「AI が止まる・疑う・分岐する」機構。将来的に `/challenge` のような単一 Skill に束ねる案あり（Claude.ai 提案）。ただし統合前に各 Issue の独自機能を言語化する必要。#15（/stop_and_report）との境界（止まる vs 疑う）も整理対象。
- **PR 運用の弊害に気づいた**：軽量なメモ・備忘も PR 必須にすると、コンテキストが溢れそうな場面で書き出す余裕を奪う。この followup.md 自体がその解決策。CLAUDE.md にも「このファイルだけは PR 不要で main 直接コミット可」のルール追記が必要（次回対応候補）。
- **PR / Issue 本文への Claude Code 署名が不統一**：コミットメッセージの Co-Authored-By は自動付与されるが、PR/Issue 本文では私の能動的な記述依存で一貫していない。プロジェクトルールとして決めるべき（「Claude Code 担当として明示する」か「一人作業の体で通す」か）。
- **`library/` はルート直下に置く（`ai-team/library/` ではない）**：`ai-team/` は他リポジトリへ持ち出すテンプレ専用で、リポジトリ固有情報を入れるとテンプレ汚染が起きる。`library/` はこのリポジトリ固有の「生物」情報スナップショット用のため、ルート直下が適切。Issue #28 の PoC 作業（[handoff/issue-28/](issue-28/)）で初版運用中。
- **#28 PR 結果レポート申し送り（処理せずに残す項目）**：①【完了 2026-04-25】 [Issue #19](https://github.com/hirobirofran/ai-team-workflow/issues/19) 本文中の `ai-team/library/` 表記を `library/` に訂正（`gh issue edit 19` 実施済み、訂正注記追加）／②【完了 2026-04-25】 [Issue #30](https://github.com/hirobirofran/ai-team-workflow/issues/30) として起票（「library/ 概念をテンプレに組込む」よりも上位の「RSS フィード相当の最新情報ウォッチ機能が欲しい」として問題提起レベルで再フレーミング。HOW は議論と整理で詰める）。

## 2026-04-25（夜・Claude.ai 返信から）

### 熟成タイミングの設計：イベント駆動 vs バッチ駆動（#21 リサーチ材料）

- **イベント駆動**（例：同種ログが N 件貯まったら抽出）: 自然な圧縮・抜け漏れリスク
- **バッチ駆動**（例：月1回/フェーズ切替時に棚卸し）: カバレッジ高・過負荷リスク
- PostgreSQL の autovacuum（自動継続）vs VACUUM FULL（手動棚卸し）の類推 → **両方要る**
- #21 自動化設計の核心論点になる

### #21 リサーチフェーズに「現オープン Issue の棚卸し」を Step 0 として含める

- 自己ナレッジループを設計する前に、**既存 Issue 群がループのどの段階で詰まっているか**を整理
- 受け皿の構造を把握してから新しいループを設計する方が筋が通る
- #22/#25/#27 の統合判断にも根拠が入る

### pull 型運用の一般原則化（library/ → knowledge.md 全体へ）

- 「困ったときに参照される側に回る、ログ蓄積の副産物として埋まっていく」方針は library/ 限定ではない
- **網羅 push は認知制約で必ず破綻する**という一般原則
- knowledge.md 自体にも適用すべき（編集者の負担管理）

### 「人間レビュアーが拾えた / AI レビュアーでは拾いにくい」パターン（#25 境界マーカー）

今回の #20 訂正往復で Claude.ai が明確にできた、**AI-hard な 3 つのレビューパターン**:

1. **主語の混同** — 私と誰かの思考の境界を見失う
2. **他者の労力の不可視化** — テンプレ化の副産物のように描いて、実は手作業の積み重ねだった点を埋没させる
3. **自己完結と外部介入の見落とし** — 人間レビュアーが拾った指摘を「自己完結」の形容で説明してしまう

→ これは Issue #25（別視点レビュー）を自動化するときの **検出できない領域の境界マーカー**。
AI レビュアーでも拾えるように設計するか、人間レビュアーをループに残すか、の判断素材になる。

### 手動コンパクトの規律（2026-04-25 観察）

- コンテキスト残 **約 40% を下回ったら**、作業途中でも次の節目で **手動コンパクト**
- **自動コンパクトを作業中に発動させない**：途中状態を圧縮で切り捨てるリスクが高い（昨日の Sonnet 切替自動コンパクト失敗の Opus 版になりうる）
- コンパクト前に必ず handoff/issue-XX/ や followup.md へ書き出し
- 「次セッションが読める情報を残してから消す」が原則

### Issue 分割の判断パターン（2026-04-25 #28 起点）

ある作業が **「上位 Issue の PoC」** と **「独立した調査・成果」** の両方の性質を持つとき：

- 独立した新 Issue を起票（メイン：独立価値、副次：上位 Issue の PoC）
- 上位 Issue にコメントで関連を明示
- `handoff/` も新 Issue 番号のフォルダで管理
- 効果：後の検索性向上、PoC ループの目的が明確、上位 Issue のスコープがクリーンに保たれる
- 実例：[#28](https://github.com/hirobirofran/eam-workflow/issues/28)（Gemini CLI DR 調査）が [#19](https://github.com/hirobirofran/ai-team-workflow/issues/19)（library/ 新設）の PoC を兼ねる構造

### ファイル参照 vs プロンプト全文貼付の使い分け（AI 別）

| AI | 推奨 | 理由 |
|----|------|------|
| Claude Code | **file-read**（パス指定だけで OK） | Read tool でローカル直接読める |
| Claude.ai Web | プロンプト全文貼付 | プロジェクト機能なし時はファイル参照不可 |
| Gemini Web | プロンプト全文貼付 | 添付しても読まないことが多い |
| NotebookLM | ソース追加 | ソース追加で読む |

→ **ファイル化 + Claude Code はファイル参照 / 他 AI は全文貼付** を運用標準とする。
　initial_prompt.md 等は両方の使い方を frontmatter に明示する（`handoff/issue-28/20260425_initial_prompt.md` が実例）。

### `ai-team/` 汚染回避とフォルダ規約の再確認（2026-04-25 #19→#28 途中で気づいた）

**`ai-team/` はテンプレ専用、リポジトリ固有情報は混ぜない**。
当初 #19 を「`ai-team/library/` 新設」と起票してしまったが、これは誤り。`library/` はルート直下が正しい。

理由:

- `ai-team/` は他リポジトリへ `cp` で持ち出すテンプレ
- リポジトリ固有情報（Gemini CLI 調査結果など）が混入すると、コピー先に持ち込まれる
- 区分：`ai-team/`（テンプレ）/ `docs/`（このリポの設計書）/ `handoff/`（このリポの作業）/ `library/`（このリポ固有の生物情報）

**運用ルールの一般化**: テンプレ自体を実験したいときは別リポジトリを切る（このリポを汚さない）。

### 整理タイミング問題への答え（2026-04-25 ユーザー問い起点）

**問題**: ログ形式の蓄積は記録しやすいが参照しにくい。気づいたら溜まり続けている。

**核心**: 「記録の容易さ」と「参照の容易さ」はトレードオフ。記録は append-only で済むが、整理は能動的判断が要る。今までの運用は前者寄り（楽な方向に流れていた）。

**処方（autovacuum 相当）**: 専用の整理タイミングを新設するより、**既存の forcing function に組み込む**。

| 既存 forcing function | 組み込む整理タスク |
|---|---|
| AAR 作成時 | 「同じトピックの log が 3 件以上あるか？」を必ずチェック→ あれば原則抽出を提案 |
| knowledge.md / followup.md に追記する瞬間 | 同じトピックの既存エントリを検索→ 似たものが既にあれば append でなく consolidate |
| PR レビュー時（`ai-team/` 変更時） | レビュー観点に「整理候補が見えるか」を1行追加 |

**3 件以上ルール**: 同一トピックが3エントリ蓄積したら、原則抽出して「ログ→原則の2段階運用」を発動する閾値。

**VACUUM FULL 相当（棚卸し）は別途必要**: Issue [#21](https://github.com/hirobirofran/eam-workflow/issues/21) の本丸として設計する。autovacuum で日常の整理、VACUUM FULL で定期的な構造再編、の二段構え（PostgreSQL 比喩そのまま）。

### CLAUDE.md PR ルール緩和（2026-04-25）

- `ai-team/` 配下のみ PR 必須（テンプレなのでレビュー対象）
- `docs/` `handoff/` `library/` は main 直接 push 可
- コミット・プッシュ確認の原則は維持（PR 経由でなくても同じ）

これは followup.md の例外運用を一般化したもの。理由：このリポジトリ固有の作業ファイルに PR を強制すると、コンテキスト保存の弊害（軽量メモを書き出す余裕が削られる）が大きい。

### トークン消費最適化の論点（2026-04-25 セッション末尾の振り返り）

**観察**: 5h 枠で 47% 消費。普段より明確に重い。原因の分析と対策候補を残す。

**主因**:

1. 大型ファイル（`knowledge.md` +266 行）への複数ラウンド編集 → 毎ラウンドでファイル全体がコンテキストに乗る
2. Issue 起票本文が人間向けで長い（#19 #25 #26 #27 #28 全て丁寧フォーマット）
3. `plan` / `plan_review` / `followup.md` の連続生成（1000+ 行）
4. PR レビューサイクルが計4ラウンドで each ラウンドで関連ファイル再読み込み

**改善候補**:

#### A. 疑似ブランチ方式（編集対象だけ抜き出す）

- 大型ファイル（knowledge.md 等）の編集時、**該当節だけを別ファイル `handoff/issue-XX/edits/<filename>_excerpt.md` に抜き出して作業**
- レビューラウンド中はその excerpt だけがコンテキストに乗る
- 合意後に元ファイルにマージ（diff 確認）
- Git の staging area 概念を認知トークン消費に適用したもの
- 懸念：「抜かなかった部分の影響」はマージ前 diff レビューで対応

#### B. ファイル分割＋インデックス化

- `knowledge.md` を `knowledge/index.md` + 主題別ファイル群に分割
  - 例: `knowledge/ai-models.md`, `knowledge/workflow-patterns.md`, `knowledge/cognitive-constraints.md`
- 編集時に該当ファイルだけ load する設計
- → Issue [#21](https://github.com/hirobirofran/eam-workflow/issues/21) の本丸（VACUUM FULL の出力形）

#### C. Issue / plan / AAR テンプレートの圧縮

- 「人間可読性 vs AI 効率」の二択ではなく、**情報量を保ちつつ密度を上げる**
- 具体: 「ぱっと思いつくこと（仮説）」セクション任意化、「想定手順チェックリスト」を別テンプレ化、表/箇条書き重視
- → Issue [#20](https://github.com/hirobirofran/eam-workflow/issues/20)（issue/PR テンプレ）の領域

#### D. NotebookLM + DR を「ソース化された再利用可能な調査結果」として活用

- DR 単体結果（テキスト）より、NotebookLM にソース投入した状態（ノート + ソース URL）の方が情報格納形式が良い
- 再質問可能 / 検証可能 / 別 AI への引き渡しが容易
- 重いリサーチは Claude Code の自前検索より NotebookLM + DR で外部計算リソースに投げる方がコスト効率良い

#### E. DR / 外部計算リソースへの分業徹底

- Claude Code 自前リサーチ：トークン枠を消費
- DR / NotebookLM：外部リソース（Claude の枠を使わない）
- **大量リサーチが要るときは DR / NotebookLM 優先、整理＆ドキュメント化を Claude Code** という分業が黄金パターン

### #28 PoC からの follow-up（2026-04-25）

[Issue #28 AAR](issue-28/20260425_aar.md) で抽出された改善候補。詳細は AAR の「改善提案」節参照。

- **Issue #25 自動化設計のテンプレ要素**: 別セッション独立コンテキスト評価 + レビュー指摘の **trace 可能化**（plan_review.md 末尾に `→ 反映済み（plan.md SectionX.Y）` 形式を追記）を必須化。実証結果は [knowledge.md「別セッション Opus によるプランレビューが効いた」節](../docs/knowledge.md) 参照
- **researcher.md フォーマット拡張案**: ① 「情報源」を**単一 URL 想定 → 複数想定**に変更、② 「**確度・要再確認ステータス**」欄を追加（preview API・need-triage Issue 等の変動要素マーカー）。library/ 昇格を見据えた拡張
- **aar.md フォーマット拡張案**: 「PoC 兼任タスクの場合の所見記載」セクション追加。今回の 4 軸（カテゴリ妥当性 / 記載フォーマット過不足 / pull 型整合 / 運用ルール不備）をテンプレ化
- **#21 で言語化すべき運用基準**: 「単発リサーチでも品質が高ければ即時昇格 OK」とするか「最低 X 日／X 件貯まってから昇格」とするかの熟成タイミング基準（既存「熟成タイミング：イベント駆動 vs バッチ駆動」エントリの具体化）
- **library/ 将来分割タイミング**: 暫定 1 ファイル `ai-tooling-ecosystem.md` で十分だったが、10 件超で `claude-code.md` `gemini-models.md` 等への分割を検討（push 的分割は不要、エントリが溜まってから）

### 「前提疑い」外部委譲手段の試運転候補（2026-04-25）

[knowledge.md「前提を疑う」が AI 単体で実施しづらい](../docs/knowledge.md) の修正版マップに対する実測候補。今日の Issue #30 起票で観察された前提崩れ ①②③ + ④⑤（Codex/Gemini 手段の誤分類）を、各手段がどこまで拾えるか試す。

- **A**: `openai/codex-plugin-cc` 経由レビュー（プラン後コーディング委譲も合わせて検証）
- **B**: ~~Gemini CLI コマンド経由レビュー~~ → 【実施済 2026-04-25】[handoff/issue-28/20260425_gemini_review.md](issue-28/20260425_gemini_review.md)。前提崩れ⑥（CWD ファイル暗黙参照）発見、独立性評価を ★★ に修正
- **C**: ~~`superpowers:code-reviewer` サブエージェント経由レビュー~~ → 【実施済 2026-04-25】[handoff/issue-28/20260425_superpowers_review.md](issue-28/20260425_superpowers_review.md)。動作確認 OK、新規指摘 6 件（うち真の見逃し 3 件）。前提崩れ⑦（誘導結果を能力と誤評価）発見
- **D**: **真の独立性試験**（B のフォローアップ）: リポジトリ外（別ディレクトリ）で Gemini CLI 実行 + plan.md だけ stdin 渡し → CWD ファイル参照を排除した状態で独立性を測る
- **E**: **Codex CLI Free 確認**: OpenAI Codex CLI が無料枠でどこまで動くか調査。動けば Codex プラグイン経由レビュー（A）と合わせて実用検証可能

### オーケストレーターセッションと実装セッションの分離（2026-04-25 起点・運用ルール）

**ユーザー指示（2026-04-25）**: 本リポジトリの長期セッション（思想議論・記録整理・handoff 準備）は **「方向性専用」のオーケストレーター役に固定**。実装タスク（Issue 単位の調査・実装・PR）は **新規セッションを立ててパス** する運用に切り替える。

**境界線**:

| やる（オーケストレーターセッション） | やらない（新規セッションへパス） |
|---|---|
| 方向性議論 | 規模ある実装・PR レベル作業 |
| 記録整理（knowledge.md / followup.md / memory）| Issue の調査・PoC |
| handoff 準備（initial_prompt.md ドラフト）| 同一 Issue の連続作業 |
| 問題提起レベルの Issue 起票 | |
| 軽い PM 判断材料の取得（gh api 1 発で読む等）| |

**理由**:

- オーケストレーターセッションのコンテキスト保護（方向性議論に集中）
- 実装セッションは fresh context で初動 → 前提崩れリスク低減（[knowledge.md「前提を疑う」が AI 単体で実施しづらい](../docs/knowledge.md) と整合）
- 「AI チーム」概念がリポジトリ内で構造化される（概念止まりだったものが運用化）

**新規セッションへのパス手順**:

1. `handoff/issue-XX/YYYYMMDD_initial_prompt.md` 作成（A/B/C の方向選択肢 + 読むものリスト + 質問例）
2. 必要に応じて `plan.md` ドラフトも併記
3. 新規セッション起動時にユーザーが「このファイルを読んで、不明点があれば聞いて、なければ状況把握レポート＋プラン提示」と指示

**確認済み事例**:

- Issue #30（テンプレに RSS ウォッチ機能が欲しい）: 切り出し済み（[`handoff/issue-30/20260425_initial_prompt.md`](issue-30/20260425_initial_prompt.md)）

**この運用自体への注意**:

- 「方向性議論」と「実装」の境界は完全には引けない（軽い判断や即時記録など）。**迷ったら『新規セッション化』に倒す**のが安全側
- オーケストレーターセッションは長期化するほど価値が上がる（リポジトリ全体の文脈を保持する）反面、コンテキスト消費が常時発生するため、定期的な手動コンパクト + 重要事項の followup.md / knowledge.md 永続化が必須

### personal-counselor の CLAUDE.md「セッション終了時の手順」を参照する（2026-04-25）

ユーザー観察（2026-04-25）: 別リポジトリ `personal-counselor` の CLAUDE.md には「**## セッション終了時の手順**」というセクションが存在する。これは Claude Code が **対話を通じて自発的にルール化していった**もので、ユーザーが明示的に指示したものではない。本リポジトリでも同種の問題（議論で合意した運用方針が永続化されない）が発生したため、向こうの構造を参考にすべき。

**取得方法**:

```bash
gh api "repos/hirobirofran/personal-counselor/contents/CLAUDE.md" --jq '.content' | base64 -d
```

**用途候補**:

- 「セッション終了時の手順」全体を本リポの CLAUDE.md に取り込む（部分採用 or 構造採用）
- `ai-team/NOTE.md` への伝播も検討（テンプレ伝播 → 他リポでも自動的に効く）
- 自発的ルール化のメカニズム自体を knowledge.md に記録（「Claude Code が運用知見をルール化する条件」）

**位置づけ**: Issue 化までは不要、次回オーケストレーターセッションで CLAUDE.md 強化作業として着手する候補。

### B（NOTE.md 拡張）の取り組み案メモ（2026-04-25）

A1〜A3 で本リポ CLAUDE.md に追加したパターン（commit 17d9559 セッション終了時の手順 / commit f6ad73e 運用改善サイクル / commit cfeb58c セッション開始時 A/B 分岐）を `ai-team/NOTE.md` にもテンプレ化したい。ただし設計原則上の制約あり。

- **B1**: セッション終了時の手順テンプレ化案
  - 本リポは `handoff/followup.md` / `docs/knowledge.md` / `handoff/issue-XX/aar.md` の 3 層
  - 他リポではどう翻訳するか（personal-counselor が単一ファイル構成なのか分割なのかは **未確認**、別途調査要）、本リポ構成準拠か、設計判断要
  - 「git push まで完了を絶対化」は共通

- **B2**: 運用改善サイクルテンプレ化案
  - 「気づきを拾う / 小さな改善はその場で / 大きな変更は Issue 化」の 3 段階閾値
  - 「META.md 相当」の置き場 = フォルダ構成再考が必要（`docs/` / `ai_memory/` 等）
  - ユーザー観察（2026-04-25）: 「最初は大げさにせず、既存との分離だけ注意」

- **設計原則：テンプレファイル直編集禁則**（ユーザー観察 2026-04-25、既存「`ai-team/` 汚染回避とフォルダ規約の再確認」エントリと近接 → **knowledge.md 昇格候補**として統合検討）
  - `ai-team/` 配下を直接書き換える運用は避ける
  - 理由：① 本リポ → 他リポへの cp 更新の差分追跡が困難 ② 他リポ → 本リポへの FB 経路が崩れる
  - **代替案候補（仮の方向性）**: 本リポ CLAUDE.md 側で NOTE.md を補足参照する形、追記型ファイル別出し、等（具体策は B2 起票時に詰める）

- **構造制約：CLAUDE.md ロード機構の階層性なし**（容認、ユーザー観察 2026-04-25）
  - 「AI チームテンプレ（触らない）→ リポ固有ルール（自由）」階層が欲しいが、global + root の 2 段が実用限界
  - 当面は CLAUDE.md に平坦に書く運用で OK、階層化は遠い将来候補

- **A3「セッション開始時 A/B 分岐」のテンプレ化スコープから外す理由**: 「`initial_prompt.md` 経由起動」は本リポ独自の `handoff/issue-XX/` 機構を前提にしており、テンプレ化しても他リポでは機能しない。NOTE.md には載せない判断（必要なら持ち込み先で個別に追加）

- **進め方**: B1/B2 は別 Issue 化（`ai-team/` 配下なので PR 必須、議論駆動）。本メモを背景参照
  - **起票判断の forcing function**: 他リポで NOTE.md を 1 回でも実適用してフィードバックが出たら起票（autovacuum 規律と整合）

### CLAUDE.md = 業務手順書という見方（2026-04-25）

ユーザー指摘（2026-04-25）「業務手順書 / トークスクリプト作っているみたい」。CLAUDE.md は AI スタッフ用業務マニュアル相当で、コールセンタートークスクリプトの設計原則（**Pull 型 / 先回り禁止 / 逐語的な開始・終了手順**）が転用できる。今日 A1〜A3 で追加したセクションはまさにこの構造（始まりと終わりを台本化、改善サイクルを明示）。**knowledge.md 昇格候補**。
