# Plan: リポジトリ整合性チェック + CLAUDE.md レビュー（2026-04-25）

## 目的

- CLAUDE.md（本リポ運用ルール）を Superpowers レビューにかけ、改善余地の所見を得る
- リポジトリ全体に対して `ai-team/templates/consistency_check.md` 相当の整合性チェックを実施し、docs ↔ 実態の乖離・ファイル間の矛盾・リンク切れ等を洗い出す
- 結果は **修正に直結させない**。所見をファイル化して AAR で申し送り（重い修正は followup.md / Issue へ）

## 背景

- 今日 A1〜A3 で CLAUDE.md を強化したばかり（commit 17d9559 / f6ad73e / cfeb58c）。直後にレビューをかけることで品質確認
- `consistency_check.md` 自体の汚染が判明（「docs ↔ 実態の乖離」が `docs/plan.md` 単一ファイル限定スコープ、例示が旧表記、`library/` 等の新要素未考慮）→ 別途 Issue 化候補
- ユーザー観察: handoff/ 配下は普段見ない、残券は Issue でしか確認しない → 本作業の所見も Issue 化必須

## 出力ファイル（すべて `handoff/misc/repo_check/` 配下）

| ファイル | 内容 |
|---|---|
| `20260425_plan.md` | 本ファイル |
| `20260425_claude_md_review.md` | Superpowers レビューのプロンプト + 結果 |
| `20260425_consistency_check.md` | 整合性チェックのプロンプト + 結果 |
| `20260425_aar.md` | 所見と申し送り（次のアクション先：followup.md / Issue 化判断） |

**プロンプトと結果を同一ファイル内に保存**：再現性検証のためレビュープロンプトは絶対残す（ユーザー指示）。

## ステップ

1. ✅ `handoff/misc/repo_check/` 作成
2. ✅ `consistency_check.md` Read（テンプレ汚染状況把握）
3. ✅ 本プラン作成 → push
4. **並列で Agent 2 つ起動**:
   - Superpowers の CLAUDE.md レビュー
   - リポジトリ整合性チェック（汚染テンプレを参考にしつつ実用判断、汚染部分は補完）
5. 各々プロンプト + 結果を保存
6. AAR 作成（出た所見 + 申し送り先：followup.md / Issue 化判断）
7. AAR ベースで Issue ドラフト起票（B1 / B2 / consistency_check 汚染 等、本文は最小・参照のみ）
8. docs/plan.md の方針確認（残券の Issue 切り出し）

## 制約

- レビュープロンプトは必ず保存（再現性検証）
- 結果ファイルから修正は今日はしない（AAR で判断）
- 長文不要、簡潔に
- B1 / B2 / consistency_check 汚染の Issue 起票は **ドラフト本文最小化**（既存メモ参照のみ）
