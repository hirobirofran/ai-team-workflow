# Issue #28 作業プラン（2026-04-25）

> **作成**: 2026-04-25 / Claude Code (Opus 4.7, 1M)
> **対応 Issue**: [#28](https://github.com/hirobirofran/ai-team-workflow/issues/28) Gemini CLI で DeepResearch ができるか調査（library/ 運用 PoC を兼ねる）
> **関連 Issue**: [#19](https://github.com/hirobirofran/ai-team-workflow/issues/19) library/ 新設
> **本ファイルの位置づけ**: 起動プロンプト（[20260425_initial_prompt.md](20260425_initial_prompt.md)）を読んだ Claude Code の状況把握＋作業計画。別セッション Opus にレビューしてもらう前提でファイル化。

---

## 1. 思想（趣旨・全体像・判断軸）

このセクションは **コンテキストが圧縮されても引き継げる** ように、なぜそうするかを残す。`docs/knowledge.md` 2026-04-25「思想の保存」教訓を反映。

### 1.1 趣旨（Why）

- **メイン**: Gemini CLI（`google-gemini/gemini-cli`）から DeepResearch を呼べるかを **一次情報で裏取りして確定** する。AI の学習データやブログ記事の孫引きを排除し、`docs/knowledge.md` の「Prisma Python クライアント実例（DR では推奨されるが GitHub ではアーカイブ済）」のような事故を避ける。
- **副次（PoC）**: 本調査の保存先を `library/`（ルート直下）にすることで、[#19](https://github.com/hirobirofran/ai-team-workflow/issues/19) で新設する `library/` の **運用ルール・カテゴリ・フォーマットを実地検証** する。不備は AAR でフィードバック。

### 1.2 全体像（フェーズ輪郭）

1. 準備（ブランチ + プラン書き起こし）
2. リサーチ（最重要 / researcher.md フォーマット / 一次情報裏取り）
3. library/ 骨格新設（index.md は提示骨格そのまま）
4. library/ai-tooling-ecosystem.md にエントリ追加
5. 構造図反映（README / CLAUDE.md / followup.md）
6. 整合性チェック・AAR
7. コミット・PR

### 1.3 判断軸（採用／不採用の理由）

| 論点 | 採用 | 不採用 | 理由 |
|------|------|--------|------|
| CLI バージョンスコープ | **最新版のみ（current state）** | 経緯込み / ローカル版起点 | ユーザー指示（2026-04-25 確認）。library/ の「時系列降順スナップショット」運用と整合。経緯は GitHub に残るため `library/` での重複回避 |
| DR 用プロンプト案の生成 | **リサーチが浅い場合のみ research.md に同梱** | 必ず生成 / 生成しない | プロンプト L49「浅ければ生成（実行は人間）」が条件付きと明示。独立成果物化すると Issue #28 のスコープが膨れる |
| PR スコープ | **library/ + 構造図 + handoff まで 1 PR** | 分割 / #19 訂正同梱 | ユーザー指示。#19 本文 path 訂正は別作業（gh issue edit）、`ai-team/` テンプレ組込みは別 Issue で扱う |
| `library/` の置き場所 | **ルート直下** | `ai-team/library/` | `ai-team/` は他リポジトリへ持ち出すテンプレ専用。リポジトリ固有情報を混ぜるとテンプレ汚染が起きる（プロンプト L33・followup.md） |
| `library/index.md` の初版 | **提示骨格をそのまま採用** | 自分で再設計 | PoC のため、提示骨格を実地で試して不備を AAR で出すのが目的。先回り改善は PoC として劣化 |
| モデル運用 | **Opus 全振り、同一セッション内で切替なし** | プラン後に Sonnet ワーカー | プロンプト L134・`docs/knowledge.md` 2026-04-25 ログ。Sonnet 200K 切替で自動コンパクト発動の前科あり |
| トークン圧迫時 | **`handoff/issue-28/20260425_checkpoint.md` に退避→新セッション** | 現セッションで巻き返す | 同上。「迷走したら新セッション」が一般化原則 |

### 1.4 セッション分離の運用ルール（再確認）

- このセッションは **本プランの作成と本作業の実行** が範囲
- 別セッション Opus に **レビュー依頼** する場合は本ファイルと initial_prompt.md を渡す（コンテキストウィンドウ独立）
- 迷走したら手元巻き戻しではなく checkpoint→新セッション（`docs/knowledge.md`「セッション分離の一般化」）

---

## 2. 作業ブランチ

`feature/issue-28-gemini-cli-dr-investigation`

main 直接コミット禁止（CLAUDE.md）。`handoff/followup.md` だけは例外（main 直 push 可、followup.md L4-5）だが本作業では使わない。

---

## 3. 手順（実行順）

### Step 1: 準備

1. ブランチ作成
2. 本ファイル（`20260425_plan.md`）が既に書き起こし済み（このファイル）

### Step 2: リサーチ（最重要フェーズ）

[ai-team/templates/researcher.md](../../ai-team/templates/researcher.md) フォーマットに従う。

**取得対象（一次情報）**:

- `google-gemini/gemini-cli` の README（最新 main + 最新 release）
- 同リポジトリの Releases / CHANGELOG（最新版を中心に）
- 同リポジトリの Issues / PRs を `deepresearch` `deep research` `grounding` などで検索（`gh search issues --repo google-gemini/gemini-cli <kw>`）
- Google AI / Gemini API 公式 docs の DeepResearch 関連
- Gemini Apps（Web 側）DeepResearch のドキュメント

**論点**:

- Gemini CLI 最新版から DeepResearch を呼ぶ手段の有無（コマンド・サブコマンド・拡張・MCP・grounding 統合など）
- 呼べない場合の代替（API grounding、Search-as-tool、別 CLI、Gemini Web 手動など）
- 一次情報 URL を必ず一覧化

**出力先**:

- 生のリサーチ記録: `handoff/issue-28/20260425_research.md`（researcher.md フォーマット）
- 熟成後の恒久情報: `library/ai-tooling-ecosystem.md`（次 Step）

**深さ判定（プロンプト L49）**:

リサーチ後、Web/gh だけでは結論が浅いと判断したら、Gemini DeepResearch 用プロンプト案を `research.md` 末尾に同梱（researcher.md「ブラッシュアップ依頼プロンプト」を参考に、ファイル内容を直接プロンプトに混ぜ込む方式）。十分なら省略。**独立成果物化はしない**。

### Step 3: library/index.md の新設

[20260425_initial_prompt.md](20260425_initial_prompt.md) L66-122 に提示された **暫定骨格をそのまま** `library/index.md` として作成。先回り改善はしない（PoC のため）。

### Step 4: library/ai-tooling-ecosystem.md の新設

提示の記載フォーマット（時系列降順、`### YYYY-MM-DD: トピック名` / 情報源 / 要点 / このリポジトリへの影響）で「2026-04-25: Gemini CLI と DeepResearch」エントリを 1 件目に記載。

### Step 5: 構造図反映（このリポジトリ用）

| ファイル | 変更内容 |
|---------|----------|
| [README.md](../../README.md) L10-17 | 構造ブロックに `library/` を追記（「このリポジトリ固有の生物情報スナップショット」と注記） |
| [CLAUDE.md](../../CLAUDE.md) L9-19 | 構造ブロックに `library/` を追記 |
| [handoff/followup.md](../followup.md) | 「`ai-team/` はテンプレ汚染回避のため、`library/` はルート」を append（規約の意図を将来セッションに残す） |

### Step 6: 整合性チェック

`ai-team/templates/consistency_check.md` を実行。未記載ファイル・矛盾を点検。

### Step 7: AAR

`handoff/issue-28/20260425_aar.md` を作成。**library/ 運用 PoC としての所見** を必ず含める:

- 暫定カテゴリ（`ai-tooling-ecosystem.md` 1 ファイル）が妥当か
- 記載フォーマット（4 項目）が実装で過不足なかったか
- pull 型運用は今回の作業フローと整合したか
- 運用ルール（PR 経由・熟成タイミング）の不備
- 改善案を `docs/plan.md` 改善候補リストか新 Issue へ昇格

### Step 8: コミット・PR

- 各 Step 完了ごとにユーザー承認を得てコミット（CLAUDE.md「コミット・プッシュ・PR は確認してから」）
- PR 作成、マージはユーザー判断
- マージ後はブランチ削除（CLAUDE.md）

---

## 4. 申し送り（PR 結果レポート / followup.md に記載）

本 PR に **含めず**、レポートで明示する項目:

1. **Issue #19 本文の path 訂正**: 本文中の `ai-team/library/` 表記を `library/` に訂正する必要あり（`gh issue edit 19`）。本セッションでは実施しない（ユーザー指示）
2. **新 Issue 候補**: `ai-team/` テンプレ（NOTE.md / templates/）に「リポジトリ固有の `library/` をルートに置く」概念を組み込む。他リポジトリへ伝播させるため別 Issue 起票

---

## 5. 重要な制約（プロンプト L131-136 を再掲）

- リサーチは Claude Code 側で完結（人間に Web ブラウザ操作を求めない）
- 一次情報 URL は最終確認のため一覧化して報告
- **同一セッション内でモデル切替しない**（Opus 全振り）
- トークン圧迫の兆候があれば `handoff/issue-28/20260425_checkpoint.md` にスナップショット保存して新セッションへ
- `handoff/followup.md` 以外は必ずブランチ + PR

---

## 6. 変更ファイル一覧（予定）

**新規**:

- `library/index.md`
- `library/ai-tooling-ecosystem.md`
- `handoff/issue-28/20260425_plan.md`（本ファイル）
- `handoff/issue-28/20260425_research.md`
- `handoff/issue-28/20260425_aar.md`

**編集**:

- `README.md` — 構造図に `library/` 追記
- `CLAUDE.md` — 構造図に `library/` 追記
- `handoff/followup.md` — 規約の意図と申し送りを append

---

## 7. 終了条件（プロンプト L138-146 + 申し送り）

- [ ] `library/ai-tooling-ecosystem.md` に「Gemini CLI と DeepResearch」節が一次情報リンク付きで埋まっている
- [ ] `library/index.md` が暫定骨格で作成済み
- [ ] `handoff/issue-28/` に plan.md / research.md / aar.md が揃っている
- [ ] AAR に library/ 運用 PoC としての所見が記載されている
- [ ] `README.md` / `CLAUDE.md` に `library/` が反映されている
- [ ] PR が作成されている
- [ ] PR 本文で「#19 本文 path 訂正」「`ai-team/` テンプレ組込み Issue 新設」を申し送り

---

## 8. Verification

- `library/index.md` 内のリンク（`ai-tooling-ecosystem.md` への相対リンク等）が解決すること
- `library/ai-tooling-ecosystem.md` の各エントリの一次情報 URL が `WebFetch` で 200 を返すこと（少なくとも本日時点）
- `consistency_check.md` の観点（未記載ファイル / 矛盾 / 整合性）に違反がないこと
- README.md / CLAUDE.md の構造図と実ディレクトリが一致すること（`ls` で照合）
- `gh pr view <PR番号>` で本文の終了条件チェックが揃っていること

---

## 9. 別セッション Opus へのレビュー依頼用ノート

このプランをレビューしてもらう際の論点候補:

- **思想と手順の両立**: Section 1（思想）が `docs/knowledge.md` 2026-04-25「思想保存」教訓に応えているか。判断軸の表（1.3）に欠けている論点はないか
- **library/ PoC としての健全性**: 提示骨格を「そのまま」採用する判断が、PoC 観点で本当に適切か。先回り改善した方が良いケースが見落とされていないか
- **暗黙前提の炙り出し**（followup.md「AI-hard レビューパターン」）: 主語の混同・他者の労力の不可視化・自己完結と外部介入の境界、の 3 観点でレビューしてほしい
- **実行可能性**: 実行者として詰まりそうな箇所、不明前提（`docs/knowledge.md`「素振り・試運転」観点）
